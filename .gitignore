# dependencies / build noise (none expected — pure static site)
node_modules/
.DS_Store

# link checker output (generated in CI)
link-report.md

# local browser-verification artifacts
.playwright-mcp/
